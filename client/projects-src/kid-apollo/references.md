# Reference library

References are encoded in `src/data/references.js`. Each item contains title, source, URL, image status, caption, extract, avoid and usage notes.

Important: verify individual licensing before using any reference in final commercial production. Linked-only references are included where embedding/reuse is unclear.
